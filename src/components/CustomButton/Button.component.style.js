import { StyleSheet } from "react-native";
export default StyleSheet.create({
  btnDefaultStyle: {
    backgroundColor: "grey",
    // borderRadius: 3,
    // borderWidth: 1,
    borderColor: "#fff"
  }
});